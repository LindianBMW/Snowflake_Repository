-- Table definition, all columns as VARCHAR for raw data ingestion

use role accountadmin;

CREATE OR REPLACE TABLE reporting.details.CLAIMS_IMPORT (
    PAID VARCHAR,
    IN_DATE VARCHAR,
    POLICY VARCHAR,
    CUSTOMER VARCHAR,
    PUR_DATE VARCHAR,
    EXP_DATE VARCHAR,
    MAKE VARCHAR,
    MODEL VARCHAR,
    REG_NO VARCHAR,
    STATUS VARCHAR,
    CATEGORY VARCHAR,
    CODE VARCHAR,
    FAULT VARCHAR,
    CAT_2 VARCHAR,
    CODE_2 VARCHAR,
    FLT2 VARCHAR,
    CAT_3 VARCHAR,
    CODE_3 VARCHAR,
    FLT3 VARCHAR,
    FLT_DATE VARCHAR,
    ASSESSOR VARCHAR,
    QT_PARTS VARCHAR,
    QT_LABOUR VARCHAR,
    AU_PARTS VARCHAR,
    AU_LABOUR VARCHAR,
    ASSESSOR_DUPLICATE VARCHAR,
    VAT VARCHAR,
    NETT VARCHAR,
    GROSS VARCHAR,
    EXCESS VARCHAR,
    REJECTION_SAVING VARCHAR,
    CONT_PCT VARCHAR,
    CHQ_DATE VARCHAR,
    NAME VARCHAR,
    MONTHS VARCHAR,
    "TYPE" VARCHAR,
    LIM VARCHAR,
    CLAIM_NO VARCHAR,
    SALE_MILEAGE VARCHAR,
    CLAIM_MILEAGE VARCHAR,
    AUTH_BY VARCHAR,
    CONTACT VARCHAR,
    AUTH_TOTAL VARCHAR,
    COSTS_QUOTED VARCHAR,
    APPROVAL_EMAIL VARCHAR,
    FIRST_REG VARCHAR,
    ACTUAL_PURCHASE VARCHAR
);

CREATE OR REPLACE TABLE reporting.details.BIGMW1_DAILY_REPORT (
    DATE VARCHAR,
    PARTNER VARCHAR,
    SUB_ACCOUNT VARCHAR,
    SCHEME VARCHAR,
    JOB_NUMBER VARCHAR,
    VRN VARCHAR,
    VEHICLE_MAKE VARCHAR,
    VEHICLE_MODEL VARCHAR,
    REPORTED_SYMPTOM VARCHAR,
    FAULT_DESCRIPTION VARCHAR,
    FAULT_CAUSE VARCHAR,
    FAULT_ACTION VARCHAR,
    RESOURCE_TYPE VARCHAR,
    JOB_TYPE_NAME VARCHAR,
    TOW_DESTINATION VARCHAR,
    TOW_DISTANCE VARCHAR,
    SERVICE_BREAKDOWN_COUNT VARCHAR
);

show tables; 