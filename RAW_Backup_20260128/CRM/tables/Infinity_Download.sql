 CREATE OR REPLACE TABLE RAW.CRM.Infinity_Download (
    rowId VARCHAR,
    triggerDatetime VARCHAR,
    igrp VARCHAR,
    dgrp VARCHAR,
    ch VARCHAR,
    src VARCHAR,
    act VARCHAR,
    algo VARCHAR,
    attr VARCHAR,
    vref VARCHAR,
    href VARCHAR,
    num VARCHAR,
    term VARCHAR,
    vid VARCHAR,
    t VARCHAR,
    goal VARCHAR,
    srcHash VARCHAR,
    new VARCHAR,
    pageTitle VARCHAR,
    pub VARCHAR,
    segment VARCHAR,
    segmentGroupId VARCHAR,
    dom VARCHAR,
    ref VARCHAR,
    network VARCHAR,
    matchRef VARCHAR,
    matchType VARCHAR,
    campaign VARCHAR,
    adGroup VARCHAR,
    adRef VARCHAR,
    keywordRef VARCHAR,
    dialledPhoneNumber VARCHAR,
    srcPhoneNumber VARCHAR,
    destPhoneNumber VARCHAR,
    callDuration VARCHAR,
    bridgeDuration VARCHAR,
    ringTime VARCHAR,
    ivrDuration VARCHAR,
    queueDuration VARCHAR,
    operatorRef VARCHAR,
    ivrRef VARCHAR,
    dialplanRef VARCHAR,
    callRating VARCHAR,
    callState VARCHAR,
    callDirection VARCHAR,
    callStage VARCHAR,
    operatorRealm VARCHAR,
    telcoCode VARCHAR,
    numType VARCHAR,
    rec VARCHAR,
    transcriptionConfidence VARCHAR,
    totalKeywordScore VARCHAR,
    totalOperatorKeywordScore VARCHAR,
    totalContactKeywordScore VARCHAR,
    operatorPositiveKeywordCount VARCHAR,
    operatorNeutralKeywordCount VARCHAR,
    operatorNegativeKeywordCount VARCHAR,
    contactPositiveKeywordCount VARCHAR,
    contactNeutralKeywordCount VARCHAR,
    contactNegativeKeywordCount VARCHAR,
    callPciDataChecked VARCHAR,
    callPciDataFound VARCHAR,
    callSsnDataChecked VARCHAR,
    callSsnDataFound VARCHAR,
    callPiiDataChecked VARCHAR,
    callPiiDataFound VARCHAR,
    callKeywordSpotting VARCHAR,
    callTranscription VARCHAR,
    whois VARCHAR,
    ip VARCHAR,
    ua VARCHAR,
    country VARCHAR,
    city VARCHAR,
    continent VARCHAR,
    res VARCHAR,
    lat VARCHAR,
    long VARCHAR,
    region VARCHAR,
    postcode VARCHAR,
    area VARCHAR,
    spider VARCHAR,
    host VARCHAR,
    visitorType VARCHAR,
    sfWhoRef VARCHAR,
    visitorPageCount VARCHAR,
    visitorGoalCount VARCHAR,
    visitorCallCount VARCHAR,
    visitorFirstDatetime VARCHAR,
    landingPageId VARCHAR,
    conversionPageId VARCHAR,
    chName VARCHAR,
    chType VARCHAR,
    segmentName VARCHAR,
    segmentRef VARCHAR,
    orgId VARCHAR,
    segmentGroupName VARCHAR,
    notes VARCHAR,
    leadScore VARCHAR,
    audit_insert_ts TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP
);

